import random
import string
malgpt
tarikdef generate_random_name():
    name_length = random.randint(5, 10)
    name = ''.join(random.choices(string.ascii_uppercase, k=name_length))
    return name

iyiydinames = [generate_random_name() for _ in range(20)]

for name in names:
    print(name)